<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.AcmePay" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/gpay_primary</item>
        <item name="colorPrimaryDark">@color/gpay_primary_dark</item>
        <item name="colorOnPrimary">@color/gpay_on_primary</item>
        <item name="android:colorBackground">@color/gpay_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Overlay theme - appears on top of other apps -->
    <style name="Theme.AcmePay.Overlay" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- No animation on activity transitions -->
    <style name="Animation.AcmePay.Overlay" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@null</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@null</item>
    </style>

    <!-- Bottom sheet style -->
    <style name="Theme.AcmePay.BottomSheet" parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/Widget.AcmePay.BottomSheet</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="Widget.AcmePay.BottomSheet" parent="Widget.Material3.BottomSheet">
        <item name="backgroundTint">@color/gpay_background</item>
        <item name="shapeAppearance">@style/ShapeAppearance.AcmePay.BottomSheet</item>
        <item name="behavior_peekHeight">auto</item>
        <item name="behavior_fitToContents">true</item>
        <item name="behavior_skipCollapsed">true</item>
    </style>

    <style name="ShapeAppearance.AcmePay.BottomSheet" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">24dp</item>
        <item name="cornerSizeTopRight">24dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <!-- Base widget style -->
    <style name="Widget.AcmePay" parent="" />

    <!-- Payment button style -->
    <style name="Widget.AcmePay.Button.Pay" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/gpay_on_primary</item>
        <item name="backgroundTint">@color/gpay_primary</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
    </style>

    <!-- Payment method card style -->
    <style name="Widget.AcmePay.PaymentMethodCard">
        <item name="android:background">@drawable/bg_payment_method</item>
        <item name="android:padding">16dp</item>
        <item name="android:layout_marginBottom">8dp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

</resources>
